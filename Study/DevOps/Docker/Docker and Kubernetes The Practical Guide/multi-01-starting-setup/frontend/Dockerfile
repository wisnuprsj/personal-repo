FROM node

WORKDIR /app

COPY package.json .

RUN npm install

COPY . .

ARG DEFAULT_PORT=3000

ARG DEFAULT_BACKEND_HOST=localhost

ENV REACT_APP_PORT $DEFAULT_PORT

ENV REACT_APP_BACKEND_HOST $DEFAULT_BACKEND_HOST

ENV CHOKIDAR_USEPOLLING true

EXPOSE $REACT_APP_PORT

CMD ["npm", "start"]